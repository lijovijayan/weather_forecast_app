export interface ICountry {
  capital: string;
  population: string;
  latlng: string;
  flag: string;
}
export interface IWeather {
    temperature: string,
        weather_icon: string,
        wind_speed: string,
        precip: string,
  }
  